#if defined(SPEC)
/* This is all that was in cctki_CartGrid3D.h, and doing this directly
 * gets rid of a filename case conflict with cctki_CARTGRID3D.h
 */
#include "cctki_COORDBASE.h"
#else
#include "cctki_CartGrid3D.h"
#endif
